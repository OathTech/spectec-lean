# Arc 2 execution log

Goal: `docs/2026-08-20_arc2-interpreter-milestone.md` (BLESSED). One-line
judgment entries, tagged [USER]/[AGENT]; checkpoints every ≤5 slices.

## Decisions

- [USER] Charter blessed 2026-08-20; long autonomous push, user sets the
  goal condition.
- [USER] Faithful-but-slow semantics (ratified earlier, carried into
  stages 4-6); luxe + genericity directives standing (objective note).
- [USER] D5 accepted: EL elaborator = named untrusted-unverified boundary;
  frontend port deliberately unscheduled until the Go-EL prototype sizes
  the EL subset (objective note D5).

## Checkpoints

- 2026-08-20: charter drafted (`32bf3d6`), blessed; log opened. Stage 1
  (D3 dump patch) next.
- [AGENT] Stage 1 encoding choices: atoms as `(a "id")`/`(s <name>)`
  (Atom.name verified 70/70 unique at pin); mixops as
  %/(brack|infix|seq …); regions `(at "file" l1 c1 l2 c2)` on the five
  def-level node types ONLY (expression-level positions deferred until an
  arc needs them — dump stays ~2.2MB vs ~5MB+); patch adds a NEW
  `--ast-structured` flag, default output untouched (so main's history
  stays reproducible); ci verifies worktree-diff == patches/*.patch
  exactly (supersedes the plain clean-tree check).
- [AGENT] Mixop-collision DONE probe implemented as build-time #guards
  (SpecTecLean/Probes.lean — labeled untrusted-evaluator TESTS) plus
  synthMixA/synthMixB in the synthetic corpus.
- 2026-08-20 CHECKPOINT stage 1 DONE: patched dump 2,218,461 bytes
  byte-identical round-trip (wasm-3.0), w1.0/w2.0 likewise; all pins
  re-pinned with reason; ci GREEN.
