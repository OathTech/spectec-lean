# Arc 2 execution log

Goal: `docs/2026-08-20_arc2-interpreter-milestone.md` (BLESSED). One-line
judgment entries, tagged [USER]/[AGENT]; checkpoints every ≤5 slices.

## Decisions

- [USER] Charter blessed 2026-08-20; long autonomous push, user sets the
  goal condition.
- [USER] Faithful-but-slow semantics (ratified earlier, carried into
  stages 4-6); luxe + genericity directives standing (objective note).
- [USER] D5 accepted: EL elaborator = named untrusted-unverified boundary;
  frontend port deliberately unscheduled until the Go-EL prototype sizes
  the EL subset (objective note D5).

## Checkpoints

- 2026-08-20: charter drafted (`32bf3d6`), blessed; log opened. Stage 1
  (D3 dump patch) next.
- [AGENT] Stage 1 encoding choices: atoms as `(a "id")`/`(s <name>)`
  (Atom.name verified 70/70 unique at pin); mixops as
  %/(brack|infix|seq …); regions `(at "file" l1 c1 l2 c2)` on the five
  def-level node types ONLY (expression-level positions deferred until an
  arc needs them — dump stays ~2.2MB vs ~5MB+); patch adds a NEW
  `--ast-structured` flag, default output untouched (so main's history
  stays reproducible); ci verifies worktree-diff == patches/*.patch
  exactly (supersedes the plain clean-tree check).
- [AGENT] Mixop-collision DONE probe implemented as build-time #guards
  (SpecTecLean/Probes.lean — labeled untrusted-evaluator TESTS) plus
  synthMixA/synthMixB in the synthetic corpus.
- 2026-08-20 CHECKPOINT stage 1 DONE: patched dump 2,218,461 bytes
  byte-identical round-trip (wasm-3.0), w1.0/w2.0 likewise; all pins
  re-pinned with reason; ci GREEN.
- [AGENT] D3b: reading subst.ml revealed exp/path TYPE NOTES
  (`(exp', typ) note_phrase`) are load-bearing for the semantic layer
  (subst.ml:195 rebuilds notes; subst_iterexp types refreshed vars from
  notes; eval.ml x17, valid.ml x5 uses). Patch extended: structured mode
  wraps every exp/path as `(! <it> <note>)`. Judged same-kind surgery as
  D3, not the escape-hatch's "larger upstream surgery". Dump: 8.65MB
  (wasm-3.0). Lean AST gains Exp/Exp', Path/Path' wrapper pairs.
- [AGENT] fresh.ml uses process-global mutable counters; the Lean mirror
  will thread counter state explicitly (pure) — alpha-variance vs OCaml
  accepted since substituted terms are never byte-compared to oracle
  output (only unmodified dumps are).
- [AGENT] `spectecil format` added; synthetic corpus content stays
  hand-authored, LAYOUT canonicalized by the tool (as before).
- 2026-08-20 CHECKPOINT stage 1b (D3b) DONE: notes dumped+mirrored, all
  corpora re-pinned (reason in pins), ci GREEN, 8,651,680 B wasm-3.0
  round-trip byte-identical.
- [AGENT] Stage 2 mirror decisions: IdSet = sorted-dedup List String (vs
  OCaml balanced-tree Set — same abstract semantics); SMap = assoc list;
  eq.ml's IfE/NegPr polymorphic-equality fallback treated as upstream
  oversight (would deep-compare regions+notes) — both implemented like
  their siblings, note-insensitive, documented in Eq.lean header,
  candidate upstream report; Mixop.eq mirrored as FLATTEN-based quotient
  (mixop.ml:54-78) not structural equality; subst's SubstM = StateT
  Fresh.St (Except String): OCaml raises/asserts (subst_gramid,
  higher-order subst, CaseE/UncaseE note asserts) fail closed;
  subst_iterexp inlined at 3 sites with the rebuilt-iter substitution
  fused (composition identical, documented).
- 2026-08-20 CHECKPOINT stage 2 DONE: Free/Eq/Fresh/Env/Subst mirrored,
  build warning-free, ci GREEN.
- [AGENT] Stages 3-4 (eval.ml + valid.ml mirrors) landed. Key mirror
  decisions: EvalM fuel-totalized (fuel = recursion depth; .fuel never
  caught); EvalErr distinguishes irred/failure/error matching OCaml's
  three exception classes and their exact catch sites;
  assume_coherent_matches fixed true; real arithmetic fails closed before
  the numeric tables; valid_atom/valid_mixop are no-ops (latex info
  undumped); proj_tup_typ's countdown-index binding mirrored verbatim
  (suspected upstream bug, candidate report); IfE/NegPr eq fallback and
  poly-eq reduction guards as documented in Eq/Eval headers.
- [AGENT] TWO mirror bugs found & fixed by validating real corpora:
  (1) Lean do-notation HOISTS `← m` out of `if A && (← m)` — OCaml &&
  short-circuits; caused an unconditional-recursion loop in equivTyp
  (grep-audited: only 2 sites, both fixed with explicit sequencing);
  (2) OCaml SHADOWS subst_typ/deftyp/exp/sym/prems with `if s = empty`
  optimization wrappers (subst.ml:280-284) — external callers never
  refresh binders under empty substitutions; our raw calls made
  reduceTypdef alpha-rename the two sides of instr <: admininstr
  differently, breaking premise equality. All Eval/Valid call sites now
  use the Opt wrappers, mirroring the shadowing.
- [AGENT] Synthetic corpus is round-trip-only in ci (hand-authored, not
  valid IL — binderless vars); the 3 real corpora are the validation
  signal; 3 mutation probes (undeclared type/var, broken cmp annotation)
  must be rejected.
- 2026-08-20 CHECKPOINT stages 2-4 DONE: binding layer + XlOps +
  Eval (1179-line eval.ml fully mirrored, fueled) + Valid (valid.ml
  fully mirrored); wasm-3.0 973/973, wasm-1.0 337/337, wasm-2.0 494/494
  defs validate (2.3s for 3.0); ci GREEN incl. validator + probes.
- [USER] Backlog: compiled-vs-slow agreement as a THEOREM (small
  compiler-correctness proof) once an il2al-style compiled path exists
  (recorded in TODO.md).
- [AGENT] Stage 6 landed: Rel.lean rule-direct derivation engine (input-
  prefix convention per relation; spec-order first-match; sequence-split
  enumeration for CatE-chain context rules; premises via matchExp'/
  reduceExp with RulePr recursion). Smoke: Step_pure [NOP] ~> [] derives.
- [AGENT] Stage 7 runner architecture: OCaml driver gains `--wast-sexpr`
  (reference-interpreter parse → AL-value command stream; Wasm 3.0
  script AST splits toplevel modules into define + `(instance)`); Lean
  side adds Al.lean (AL S-expr reader + type-directed decoder into IL
  value exps) and Runner.lean ($invoke/$instantiate as spec functions,
  Step iterated to a terminal state). Decoder rules discovered against
  the corpus: construct.ml names cases by ANY atom ("->"), atom-less
  cases are `CaseV ""`, notation singletons (list(X), char, name) wrap,
  TextV decodes to codepoint lists.
- [AGENT] isNormalExp fast path in reduceExp (perf divergence, logged
  in-code): eval.ml re-checks case premises on every CaseE reduce —
  quadratic on value-sized terms. Refined to isPureNormalExp (subE
  excluded): treating `subE(value)` as normal skipped eval.ml:430-433's
  subsumption ERASURE, leaving SubE wrappers in bound values and
  breaking every downstream match (root cause of the first
  instantiation failure).
- [AGENT] Four upstream/spec findings extracted to docs/findings/
  ([USER] directive 2026-08-21): IterE-opt all-some reduces UNWRAPPED
  (ill-typed; our wrap divergence documented in-code);
  reduce_exp_call substitutes bodies with arg bindings only (premise
  bindings lost; we thread them); $allocmodule's premise order is not
  executable as written (forward guess; upstream animates in il2al —
  we animate dynamically); eta_iter_exp `assert false` reachable via
  unoriented binding equations (we throw Irred).
- [AGENT] Engine-level rules added to execute raw IL (all logged
  in-code at their sites): binding-if equations executed as pattern
  matches in either orientation, with a GROUND-solution gate (open
  bindings defer); worklist premise deferral with progress rounds
  (dynamic animation); rel-premised calls ($evalglobals, $evalexprs)
  evaluated via evalCallRelA/rewriteCalls (eval.ml's reduce_prem
  returns unknown for RulePr — Rel recurses instead); `Steps` derived
  as Step's reflexive-transitive closure iterated to exhaustion
  (first-match search would stop at steps-refl); CPS input matching so
  sequence-split enumeration backtracks on premise failure (ctxt-instrs
  commits only when the whole rule succeeds); nested split decomposition
  through config cases; assumed-relations harness boundary
  (Module_ok/Externaddr_ok) unchanged from the earlier decision.
- [AGENT] Perf work (all semantics-preserving, measured): reduceExpCall
  memo keyed (defid, reduced args) in Fresh.St (~50x on nop
  instantiation path); derive memo keyed (relation, inputs) — sound
  because cycle-guard refusals only prune cyclic derivations, which LFP
  never needs (killed the exponential split-enumeration on no-rule
  confirmation); Step-family rule pruning by anchor atom (skips only
  provably-non-matching rules); values-first terminal shortcut in
  runConfig; ptrEq short-circuit in eqExp via implemented_by (pointer
  equality implies structural identity; pure contract unchanged).
  REJECTED after measurement: pointer-keyed memo for isPureNormalExp
  (two attempts regressed mini 250x for causes not fully diagnosed —
  reverted; bench in isolation says the mechanism is sound, so this
  remains a lead for a future perf arc, not a claim).
- [AGENT] 2026-08-21 EARLY-EXIT assessment (charter escape hatch): the
  runner is CORRECT on the pilot corpus (mini.wast 3/3: define,
  instantiate, invoke/assert_return pass through real spec rules;
  nop.wast module instantiates and asserts execute) but throughput is
  ~1s/Step and minutes/instantiation. The full-suite DONE (19,841
  assertions, 20-file pass floor) is computationally out of reach in
  this architecture: the remaining bottleneck is O(|store|) term walks
  inherent to store-in-term rewriting without hash-consing. The two
  sound escalations — pervasive hash-consing of the AST, or upstream-
  AL-style store indirection — are architectural, next-arc decisions
  that trade against the "execute the rules directly" fidelity bet this
  arc was chartered to test. Declaring the early exit rather than
  weakening the gate or the claims.
- 2026-08-21 CHECKPOINT stage 7 + ARC END (EARLY EXIT): driver patch
  (7a), engine rules + memoization (7b), AL decoder + runner + ci pilot
  (7c), findings + records (7d), cache bounds + probes 4-5 (7e),
  Results + audit ask committed. ci GREEN on the final tree (5/5
  validator probes, 3/3 fail-closed probes, wast pilot baseline).
  Exit invoked per the charter's stage-6/7 escape hatch with
  measurements; branch parked UNMERGED awaiting the user's audit
  sign-off (merge is the user's, always).
- [AGENT] 2026-08-21 post-checkpoint: the background full-nop.wast run
  was KILLED at 31 min / 53 GB RSS, still inside instantiation (memory
  growth persists despite the cache epoch flushes — suspect symbolic
  intermediate terms under premise deferral; shared-machine safety
  required the kill). Results section amended: no nop.rows artifact
  exists, none claimed. This is additional exit evidence: instantiation
  cost is super-linear in module size in both time AND space.
- [AGENT] 2026-08-21 GOAL-LEVEL EMERGENCY EARLY EXIT (distinct from the
  charter escape hatch already invoked; declared per the goal's own
  emergency mechanism, which requires stating the nature):
  NATURE: (1) blocked by lack of resources — the two remaining DONE
  items (19,841/19,841 classification; 20-file pass floor) require
  compute measured at minutes-to-hours per module instantiation and
  ~1 s/Step, extrapolating to multi-day-to-week suite runs, with an
  observed 53 GB RSS on a single mid-size file that endangers the
  shared machine other agents depend on; (2) uncompletable as
  chartered — restoring feasibility requires architectural changes
  (hash-consed AST or AL-style store indirection or the il2al port)
  that the charter's hard boundaries exclude from this arc ("No AL/
  il2al port"; faithful rule-direct execution is the arc's bet), and
  the charter's own stage-6/7 escape hatch prescribes parking with
  measurements for exactly this case. Continuing would be spinning
  with minimal progress against a DONE the measurements show is
  unreachable in-architecture. All completed work is committed, ci
  GREEN on the final tree, records current, audit ask posed, branch
  parked unmerged.
- [AGENT] 2026-08-21 exit REAFFIRMED after adversarial self-check
  prompted by the goal monitor. Re-examined whether any honest
  in-architecture path completes the remaining DONE items: (a) the
  19,841-assertion classification COULD be mechanically produced by
  running all 97 files at low fuel and classifying overruns as an
  explicit `fuel` class — visibly-can't-handle, not a false pass — but
  the result would be a ~all-fuel table costing hours of shared-machine
  compute while adding almost no signal beyond the driver-boundary
  counts already recorded; judged gate-theater and rejected; (b) the
  pass floor (~2,400 assertions REQUIRED TO PASS across 20 files whose
  modules are larger than nop.wast, which itself exceeds 31 min / 53 GB
  in instantiation alone) has NO honest path at current throughput —
  classification tricks cannot make an assertion pass. The emergency
  exit therefore stands on the goal's own criteria: blocked by lack of
  resources, and uncompletable within the charter's hard boundaries
  (the enabling rewrites are excluded from this arc). Per the goal
  text, an emergency exit is always permitted once its nature is
  declared; nature declared above.
- [AGENT] 2026-08-21 record CORRECTION during exit review (honesty over
  tidiness): the reaffirmation entry overstated one point. Of the three
  enabling rewrites, only the il2al/AL port is excluded by the
  charter's hard-boundary text; store indirection is excluded by the
  arc's fidelity bet (arguably arc-level, not textual); HASH-CONSING
  the IL AST is NOT charter-excluded — it is judged INSUFFICIENT, not
  forbidden. Basis: (1) two lighter pointer-identity prototypes
  regressed ~250x in vivo for causes not fully diagnosed — a 1-3 day
  full hash-consing refactor on that foundation is high-risk; (2) its
  expected gain (O(1) eq/hash, term dedup) attacks comparison and
  storage cost but not entry counts, substitution churn, or worklist
  retries — estimated 10-50x where 100-1000x is needed; and (3) the
  pass floor additionally requires real wasm NUMERICS, which the spec
  defines via bit-level list-manipulating def functions — rule-direct
  execution of e.g. i32.add costs hundreds of reduction steps PER
  ARITHMETIC OP, putting i32.wast-class files further orders of
  magnitude away regardless of hash-consing. The emergency exit stands
  on the corrected rationale: resource-blocked, and mis-scoped DONE
  for this architecture (the contingency the charter's escape hatch
  was written for).
- [AGENT] 2026-08-21 post-exit continuation: REVISED the earlier
  "gate-theater" judgment about the classification DONE item, prompted
  by the goal monitor's insistence. On re-examination, a corpus-wide
  classification with honest explicit classes IS the charter's
  guardrail corpus (visibly-can't-handle, never a false pass) and the
  pinned differential baseline arc 3 diffs against — value on its own
  merits, cheap at a per-file wall budget. Executed:
  scripts/wast-classify (120 s/file, 16G cap, sequential — machine
  stayed at ~6G used) + scripts/wast-rollup →
  baselines/wast-differential.tsv. RESULT: 20,029/20,029 assertions
  across 97/97 files classified, zero unclassified, zero false passes;
  169 real passes (const.wast dominates), 18 visible fail-cascades from
  errored grow actions, remainder resource-bounded or
  driver-unsupported (counts in the baseline header). Driver NullLit
  crash found by the sweep and fixed (patch regenerated; 4 files
  recovered). Charter Results scorecard updated: classification item
  MET (with the honest-classes caveat stated), unsupported enumeration
  MET; the PASS FLOOR remains NOT MET and the emergency exit stands
  for that item alone, on the unchanged evidence (throughput +
  numerics orders-of-magnitude gap).
- [AGENT] 2026-08-21 pass-floor last-shot experiment (prompted by the
  goal monitor's continued insistence): hypothesized the instantiation
  blowup was dominated by SYMBOLIC evaluation of open-argument calls
  (the 53 GB $allocfuncs-with-free-moduleinst attempt), which the
  ground-solution policy renders pure waste — added an OPEN-ARG GUARD
  (reduceExp CallE row + evalCallRelA): calls whose reduced arguments
  contain free variables stay symbolic; the worklist retries them once
  ground. RESULT: mini unchanged (0.5 s, ci pilot green); nop.wast
  instantiation STILL exceeds 5 min — the dominant cost is confirmed to
  be O(N²) store growth (each funcinst embeds the full moduleinst) ×
  O(|store|) reduction walks, not the symbolic attempts. Guard KEPT for
  its memory-safety and waste-elimination value (documented at the
  site). Because it changes engine behavior, the corpus baseline is
  being REGENERATED under the guarded engine before re-pinning
  (deliberate re-pin, reason: engine change; pre-guard copy retained
  for the diff). The pass floor remains unreachable; the emergency exit
  for that sole item is FINAL on this evidence.
- [USER] 2026-08-21: arc-3a and arc-3b charters BLESSED as drafted;
  arc-2 audit plan APPROVED (4 adversarial reviewers on dims 1-4 +
  independent verify pass + orchestrator-inline gate-integrity dim);
  sequencing approved: sweep → arc-2 finalization → audit → merge
  sign-off → both lanes spawn off main. Orchestrator working mode
  adopted as standing practice.
- [AGENT] 2026-08-21 stage 7g CORRECTION (gate caught a real mirror
  violation; commit fb404e9's "Verified: ci GREEN" claim was FALSE):
  the unconditional open-arg guard broke wasm-3.0 VALIDATION — eval.ml
  DOES reduce open-arg calls and valid.ml depends on it; the guard in
  the shared reduction path was an undocumented divergence, and ci's
  validator step failed. The false verification claim came from piping
  ci through `tail` and testing the pipe's status (tail's), not ci's —
  process error, mine; ci status is now checked directly. FIX: guard
  scoped behind `Env.guardOpenCalls` (default FALSE; documented at the
  field), set only by the run-wast execution path. Validation restored;
  ci re-run GREEN with exit status checked; the re-pinned 184-pass
  baseline remains valid (execution behavior unchanged by the scoping).
- [AGENT] 2026-08-21 audit-response (arc2-fix lane) begins; fixes keyed
  to the audit findings (dims 1-4 + verification pass, all confirmed).
- [AGENT] fix 1 (BLOCKER dim2-1/V1): matchSeqK fixed components are
  list GROUPS in elaborated IL — now consume groupLen(p) elements and
  match as a list; undeterminable lengths FAIL CLOSED. Validated: the
  audit counterexample `(i32.const 1) unreachable` under assert_trap
  flips stuck→pass (Step_pure/trap-instrs now derivable); `unreachable
  nop` passes; mini pilot unchanged; ci GREEN.
- [AGENT] fix 2+3 (dim2-4/V3, dim2-5, dim1-2/V9): three-valued premise
  checking. Rel.checkPrems now returns PremsRes (ok/fail/unknown):
  boolE-false, iter-.no, closed-equation definite mismatch (no Irred,
  both sides closed+normal), closed-rulePr noRule, and output-pattern
  mismatch are FAIL; Irred-involved equations, closed undecidables
  (corrected from the previous fail), stuck sub-derivations, and
  deferral exhaustion are UNKNOWN. evalCallClauses mirrors
  eval.ml:527-530 exactly (unknown ⇒ whole call irreducible, surfaced
  as a caught-or-visible error; only definitive fail advances clauses).
  deriveCore threads sawUnknown through the rule scan; an
  elsePr-carrying (otherwise) rule after an undecidable sibling returns
  visible .stuck instead of firing (unknown squeezed through the
  Option-typed split enumeration via a saved/reset/restored
  Fresh.St.premsUnknown scratch flag). Eval.reducePrem's negPr inverts
  only DECIDED truth: a guessed-witness .yes (non-empty substitution)
  yields .unknown, never .no (eval.ml:556-561 extension documented).
  NOTE: the dead anchor-skip logic in deriveCore was removed as part of
  this rewrite (region overlap); its helper defs go in the removals
  slice. Validated: mini/v1a/v1c pass; ci GREEN (exit 0).
- [AGENT] fix 4 (dim1-1/V5): hasVar* completeness — the ground-gate's
  freeness test now covers all positions (update/extend paths incl.
  idxP/sliceP exps, listN length exps, subE types, typA/gramA call
  args via full Typ/Sym traversals, rulePr args, iterPr domains),
  cross-referenced to Free.freeExp as the reference definition. Total
  (attach/termination_by pattern); ci hygiene green. mini/v1a pass;
  ci GREEN.
- [AGENT] fix 5 (dim2-3/V2): imports FAIL CLOSED. The harness has no
  linker; instantiating import-declaring modules with an empty
  externaddr vector mis-indexed exports (demonstrated silent wrong
  answer, verified V2). A decoded module whose list(import) component
  (MODULE payload index 1, through the singleton-notation wrapper) is
  non-empty now classifies `unsupported:imports` and never
  instantiates; malformed shapes error. Validated: the V2 module gives
  unsupported:imports + error rows (no pass/fail); mini unchanged; ci
  GREEN.
- [AGENT] fixes 6+7+10+11 (dim3-F1/V10, dim2-2/V8, dim1-7/dim3-F6,
  dim3-F2): deriveCache inserts ONLY refusal-free (empty-visited)
  computations — lookups stay unconditional (a cached entry is the
  guard-free result); Fresh.lean invariant rewritten (the prior
  visited-independence claim was false). derive's docstring now states
  the multi-output limitation (Ref_ok/sub etc.) and inputArity carries
  a registration warning gating it. Rel.evalCallRelA's open-arg guard
  gated on Env.guardOpenCalls (the copy f0afbcc missed). callCache key
  extended with guardOpenCalls; one-Env-per-process assumption and the
  alpha-variant/fuel-onset caveats documented at the cache. mini/v1a
  pass; ci GREEN.
