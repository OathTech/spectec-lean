# Arc 1 execution log (autonomous run, started 2026-08-20)

Goal: complete `docs/2026-08-20_arc1-il-bridge.md` DONE, entirely. One-line
judgment-call entries, tagged [USER] (user-decided) or [AGENT]
(agent-decided). Checkpoint entries every ≤5 slices.

## Decisions

- [USER] Arc-1 scope blessed incl. byte-identical PP round-trip ("Add the
  PP too, let's do it properly", 2026-08-20).
- [USER] Autonomous completion of the whole charter; agent makes and logs
  judgment calls; no merge; emergency exit available (goal-set message,
  2026-08-20).
- [AGENT] Mixops/atoms in the Lean IL AST are opaque dump-level token
  strings: `Mixop.to_string` (xl/mixop.ml:91-93) is a lossy, non-injective
  flattening, so reconstructing mixop structure from the dump would be
  invention (park-don't-improvise). Structured mixops recorded as an open
  question for a later arc (needs a structural dump upstream or our own
  printer patch).
- [AGENT] Positions (`.at` regions) and hints are absent from the Lean AST:
  backend-ast/print.ml prints `.it` only and drops `_hints` (print.ml:89,92)
  and HintD (print.ml:220-221); the dump carries neither.
- [AGENT] Scratch outputs live in repo-local `artifacts/` (gitignored):
  /tmp is write-only under the nono profile, and repo-local is better
  discipline anyway.
- [AGENT] Parser totality via bounded-index recursion with explicit
  "did not advance" internal-error guards (fail-closed loud sentinel, the
  cerberus arc-3 fuel pattern) rather than carrying monotonicity proofs
  through the parser API; termination measures are `tokens.size - i`
  (2*(n-i) / 2*(n-j)+1 for the mutual pair).
- [AGENT] Input required to be printable ASCII (tokenizer fails closed
  otherwise): all `pp` output is (String.escaped maps the rest away), and
  this makes Lean's char-based `String.length` coincide with OCaml's
  byte-based one, which the layout arithmetic depends on.
- [AGENT] `NumE` Real values will carry the raw printed token (Lean cannot
  reproduce OCaml `%.17g` formatting); zero reals occur in the wasm-3.0
  dump (grep-verified) so the branch is exercised never; documented
  in-code.

- [AGENT] Literal canonicality checks in the reader: every numeric/quoted
  token is re-rendered after parsing and must equal the source token, so
  `print ∘ parse = id` holds token-wise by construction (rejects `\065`,
  `007`, lowercase hex, `-0` — none of which print.ml can emit).
- [AGENT] `iterexp` reading inlined at its three use sites (exp/sym/prem
  `iter` nodes) rather than a shared list-consuming helper — keeps
  termination structural; printer side keeps the shared helper like
  print.ml:141-142.
- [AGENT] `TypBind`/`Dom` wrapper inductives for ast.ml's anonymous tuples
  (Lean structural-recursion idiom; documented in Ast header).
- [AGENT] `projE`/`numG` carry `Nat` where OCaml has `int`: the printer
  formats (`string_of_int` / `0x%02X`) make negatives unrepresentable or
  garbage; reader fails closed on a negative. Documented in Ast.lean.
- [AGENT] ci includes three fail-closed probes (corrupted dumps must be
  rejected) — deterrent standard, marked do-not-harden in the script.
- [AGENT] wasm-1.0/2.0 dumps added to ci as roundtrip-only checks (no
  extra sha pins — the spectec commit pin already fixes their content).
- [AGENT] ci requires the build warning-free? No — it REPORTS warnings but
  only errors fail the gate; the arc ends warning-free as a matter of
  fact, not gate law.

## Checkpoints

- 2026-08-20: slice 1 committed (charter + pins, `e49fee7`).
- 2026-08-20: slices 2-3 committed (`09a720d`) — skeleton + Sexpr layer,
  text→Sexpr→text byte-identical (973 sexprs / 1,549,779 bytes).
- 2026-08-20: slices 4-5 committed (`8bdbc25`) — IL AST + reader/printer,
  full round-trip byte-identical; 3 corruption probes fail closed.
- 2026-08-20: slice 6 committed (`f276e1f`) — scripts/ci GREEN.
- 2026-08-20: ARC COMPLETE — extra corpora (wasm-1.0/2.0) byte-identical
  and added to ci; charter Results written; audit ask posed. Branch
  parked at branch-complete, no merge (user's call).

## Post-arc addendum (2026-08-20, after branch-complete)

- [USER] Objective ratified: generic SpecTec-in-Lean toolkit; first client
  = GoLean frontend-soundness theorem. Standing directives: "luxe"
  (strongest/most far-reaching design choices) and genericity (no Go
  coupling in the toolkit core). Decisions D1-D4 recorded in
  docs/2026-08-20_objective-go-frontend-soundness.md.
- [AGENT] The objective note + CLAUDE.md goal rewrite are committed on the
  arc-1 branch AFTER the audit-scope commit cea4854 (docs-only delta;
  flagged for the merge report so the audited state vs merge state
  difference is visible).

## Audit corrections (2026-08-20, post-review — see docs/2026-08-20_arc1-audit.md)

- CORRECTION to the canonicality entry above: as written it claimed every
  numeric token was re-render-checked; the audit (A1/B3) showed the int
  branch was not, and the real branch trivially so. Fixed in code (int
  render-compare; real fails closed); entry retained unedited above per
  record-integrity practice.
- CORRECTION to the projE/numG entry above: "Documented in Ast.lean" was
  false for projE and thin for numG (A3/B9); the "fails closed on a
  negative" rationale was false for numG's two's-complement rendering
  (A4). Now actually documented in the Ast divergence header, with an
  OCaml max_int bound enforced in the reader.
- [AGENT] Audit fix slate applied on-branch (12 findings: 3 MAJOR fixed,
  8 MINOR fixed, 1 documented); synthetic coverage corpus added;
  wasm-1.0/2.0 sha-pinned; gate GREEN post-fix. Disposition table in the
  audit record.
