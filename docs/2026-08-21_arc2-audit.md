# Arc-2 audit record (2026-08-21)

Plan approved by user 2026-08-21: four decorrelated adversarial
reviewers (Opus-class, skeptical persona, primary sources, file:line
grounding) on dimensions 1-4; gate integrity (dim 5) reviewed
orchestrator-inline at deterrent standard; one independent verification
pass (default-refute) over the ten highest-severity findings; synthesis
with self-spot-checks. Audited tree: `f0afbcc`.

## Verification outcome

10/10 verified findings CONFIRMED, zero overclaims. Both executable
counterexamples reproduced end-to-end; both numeric claims reproduced
to the digit.

## Findings (verified severity order) and dispositions

1. **BLOCKER** `matchSeqK` matched fixed sequence components
   element-wise, but elaborated IL makes literal instruction groups
   `listE`s — `Step_pure/trap-instrs` and `Step_read/throw_ref-instrs`
   were unreachable (live counterexample: values-then-trap inside a
   frame went stuck). FIXED `a4851ec` (group-length matching,
   fail-closed on undeterminable lengths); counterexample now passes.
2. **MAJOR** Assumed-relations boundary produced a silent wrong
   answer: modules with declared imports instantiated against an empty
   externaddr vector, mis-indexing exports (demonstrated wrong-function
   `pass`). FIXED `828791f`: imports now fail closed as
   `unsupported:imports`.
3. **MAJOR** `Rel.evalCallClauses` conflated undecidable premises with
   definite clause failure (eval.ml:527-530 distinguishes them),
   risking wrong `-- otherwise` selection; same collapse at rule level.
   FIXED `4564a2c`: three-valued premise results; unknown makes calls
   irreducible; elsePr rules blocked (visible `.stuck`) after an
   undecidable sibling.
4. **MAJOR** `negPr` inverted a guessed-witness equality into definite
   falsity (latent; zero corpus `neg` premises). FIXED `4564a2c`
   (witnessed yes → unknown).
5. **MAJOR** `deriveCache`'s visited-independence argument had a
   transitive-contamination hole (schema confirmed; no wasm instance).
   FIXED `dd9e466`: only empty-visited computations are inserted.
6. **MAJOR** The visited-guard "semantically exact" docstring is false
   for multi-output relations (`Ref_ok/sub`,
   4.1-execution.values.spectec:65-69), currently contained by
   `inputArity` failing closed. DOCUMENTED `dd9e466` (guard limitation
   + warning at inputArity); engine fix deferred to the arc that
   extends inputArity.
7. **MAJOR** The `eqExp` ptrEq `implemented_by` never applied to
   recursive calls (proven from generated C) — the in-code claim and
   two record entries were false. REMOVED `911bdcc`; unsafe surface
   now empty; ci hygiene grep extended to forbid
   `unsafe`/`implemented_by`.
8. **MAJOR (records)** Results cited superseded pre-guard counts
   (169/11,342/4,822/66 vs pinned 184/11,346/4,802/67); "error =
   instantiation-timeout cascades" mischaracterized 11,321 rows that
   are `entry call invoke: no clause applies` AFTER successful
   instantiation (99.6% of error rows); a charter sentence overstated
   nop.wast ("first asserts pass" — baseline shows none); fail=18
   characterization wrong for 2/18. CORRECTED in the Results rewrite
   accompanying the post-fix baseline re-pin.
9. **PROTOCOL** All arc-2 work was committed directly on `main` (the
   charter branch never existed) and stage 6 was pushed; "branch
   parked" records were false, inherited from an unverified session
   summary. USER DISPOSITION: no git surgery; fixes on `arc2-fix`;
   branches-only made an absolute rule (CLAUDE.md, `f1cdce7`).
10. **MINOR/NOTE (selection)** `hasVarExp` missed six variable
    positions (FIXED `28d7ca8`); anchor pruning was dead code — anchor
    `none` for all 108 Step-family rules (REMOVED `911bdcc`);
    `evalCallRelA`'s guard copy unconditional (FIXED `dd9e466`);
    callCache key omitted the engine mode (FIXED `dd9e466`); fast-path
    /etaIterExp divergence notes had wrong rationales (FIXED
    `2e6814e`); memo hits skip fresh bumps — alpha-variance +
    `.fuel`-onset history-dependence documented (`dd9e466`);
    checkpoint-cadence gap (6 slices once) and corpus-path naming
    (deps/wasm-spec vs charter's deps/spectec path) acknowledged here.

## Dimension-5 (gate integrity, deterrent standard)

Pilot step catches driver/runner/decoder drift via baseline diff;
probes fail closed if their mutations stop matching; both baseline
re-pins carried committed reasons. Hardening applied post-audit:
hygiene grep extension (`911bdcc`). Residual note: ci step 7's pipe
masks the runner's own exit status (the diff still catches damage).

## Unfixed items carried forward

- Multi-output relation derivation (finding 6) — blocked-by-design
  until inputArity grows; warning comments in place.
- The 11,321-row `invoke: no clause applies` engine gap — NOT an audit
  fix item; first diagnostic target of arc 3a.
- Cache keys still omit `Env` beyond the mode bool — documented
  one-env-per-process assumption.

Reviewer/verifier cost: 5 subagents, ~0.77M subagent tokens.
