# Arc-1 pre-merge audit: synthesis and disposition (2026-08-20)

Plan (approved by Mike): two decorrelated adversarial Opus reviewers —
A: OCaml↔Lean correspondence; B: fail-closed coverage, dispatch soundness,
gate honesty — findings verified per the five-step pattern. Audited state:
`cea4854`. Both reviewers ran the gate independently (GREEN) and confined
scratch to `artifacts/`; neither modified tracked files.

## Headline

**No BLOCKER.** The hunted class — consistent reader+printer errors
invisible to the byte-identical gate — was verified clean by A
(constructor-by-constructor routing check, both directions) and
independently by B (Python mirror of print.ml fuzzing the Lean reader:
2,300 random printer-shaped scripts, 0 failures; ~1.1M-sample printer-
injectivity scan, 0 collisions). A verified `OcamlEscape` byte-for-byte
against real OCaml 5.4.0 `String.escaped` (all 256 bytes + multibyte),
and every `file:line` citation except one (F-9).

## Findings and disposition (all fixed on branch unless noted)

| # | Sev | Finding (reviewer) | Disposition |
|---|-----|--------------------|-------------|
| F-1 | MAJOR | `(rat 4/2)`, `(rat 1/0)`, `(rat 0/0)` accepted AND re-print identically — the one genuinely silent class (B1; A2 non-silent variant). Self-spot-checked: confirmed. | FIXED: reader enforces Q.t canonicity (den > 0, reduced) + render-compare. Probes now reject with located errors. |
| F-2 | MAJOR | int branch skipped the canonicality re-render; `(int +007)` accepted; three in-code/record claims thereby false (A1; B3). Self-spot-checked: confirmed. | FIXED: render-compare added; claims corrected (OfSexpr header, readNum docstring, log correction below). |
| F-3 | MAJOR | Gate blind spot: 7 reader/printer pairs occur zero times in all wasm corpora (int, rat, real, unopt, let, neg, impl) while the record cites the round-trip as fidelity evidence (B2). | FIXED: tracked synthetic corpus `baselines/synthetic-coverage.sexpr` exercising all wasm-absent branches except `real`, wired into ci (5c). `real` now fails closed (below). |
| F-4 | MINOR | `(real <anything>)` accepted verbatim, unconstrained and untestable (B4; A "could not verify" 1). | FIXED: real literals now FAIL CLOSED with a dated error; extend when a corpus needs them. Printer keeps the mirror case. |
| F-5 | MINOR | Tokenizer skipped the printable-ASCII check on the escaped byte, breaking the header's load-bearing ASCII invariant (A5; B5). | FIXED: escaped byte now range-checked; invariant holds on every path. |
| F-6 | MINOR | `String.fromUTF8!` panic reachable on the diff-report path (B6, via F-5). | FIXED: total decode with byte-list fallback. |
| F-7 | MINOR | ci step 3 latent SIGPIPE exit 141 on warning-heavy builds (false red, never false green) (B7). | FIXED: `|| true` on the head pipeline. |
| F-8 | MINOR | wasm-1.0/2.0 dumps had no content pin; commit pin ignores dirty checkout (B8). | FIXED: sha pins added for both + `git diff --quiet` worktree check. Untracked files under deps remain uncovered (noted in the check's message). |
| F-9 | MINOR | Stale citation `config.ml:10` (file has 9 lines) (A7). | FIXED: `:9`. |
| F-10 | MINOR | `projE`/`numG` Nat-vs-int divergence undocumented in the Ast divergence header; log claimed it was; `numG` accepted 63-bit two's-complement renderings of negative ints (latent, unreachable at pin) and values beyond OCaml max_int (A3, A4, B9). | FIXED: divergence documented in Ast header; OCaml `max_int` bound enforced in `readHexByte` and proj index (closes the A4 latent hole). |
| F-11 | MINOR | `Atom ""` (nested-HintD rendering) is invisible to any tokenizer; header's fail-closed claim overstated (A6, B "verified clean" trace: always a VISIBLE diff, believed unreachable per il/free.ml:213 + elab.ml:2853-2859) | DOCUMENTED as a known boundary in the Sexpr header (cannot be detected at parse; can never pass silently). |
| F-12 | MINOR | Hygiene grep gaps, forward-looking (B10). | PARTIAL: `set_option maxHeartbeats/maxRecDepth` added (CLAUDE.md names them defects). NOT added: panic!/fromUTF8!/unsafe/axiom — deterrent standard, revisit when proofs start (Audit.lean pattern is the real mechanism). |

## Carried forward (could-not-verify, both reviewers)

1. No AST-level differential vs OCaml exists (byte-identity + injectivity
   evidence stands in); a `print-il`-vs-ours differential or a
   `readScript ∘ scriptToSexprs = id` theorem would settle it — natural
   arc-3+ item.
2. Reachability of nested-HintD `Atom ""` and negative `NumG` argued by
   source trace, not constructed counterexample (both now defended in
   depth regardless).
3. Real literals: no corpus, now rejected — decision forced if ever hit.
4. `scripts/capped`/`env.sh` were outside both dimensions.

## Post-fix state

All fixes verified by re-running the probes (F-1/F-2/F-4/F-5 inputs now
rejected with located errors, no panic) and `scripts/ci` → GREEN,
warning-free, including the new 5b sha pins and 5c synthetic corpus.

Log corrections: two [AGENT] entries in docs/2026-08-20_arc1-log.md
(canonicality claim; "documented in Ast.lean" claim) were falsified by
this audit — corrective entries appended there; originals left in place.
